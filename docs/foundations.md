---
title: Foundations
---

# Foundations and Definitions

<div class="intro-card">
This page collects the main objects that the formalization is built on: the
polynomial ring, the binomial edge ideal, the closed-graph condition, the
project's monomial order, and the core Gröbner-basis API. It is the right entry
point if you want the vocabulary of the development before reading the section
pages.
</div>

## Core Algebraic Setup

| Mathematical object | Lean declaration(s) | File | Notes |
|---|---|---|---|
| Variable type $x_i, y_i$ | `BinomialEdgeVars` | [Definitions.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/Definitions.lean) | Implemented as `V ⊕ V`, with `Sum.inl i` for `x_i` and `Sum.inr i` for `y_i` |
| Coordinate functions | `x`, `y` | [Definitions.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/Definitions.lean) | Shorthand for the coordinate variables in `MvPolynomial (BinomialEdgeVars V) K` |
| Binomial edge ideal $J_G$ | `binomialEdgeIdeal` | [Definitions.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/Definitions.lean) | The ideal generated by the binomials $x_i y_j - x_j y_i$ for edges $\{i,j\}$ with `i < j` |
| Quadratic generators $f_{ij}$ | `fij`, `generatorSet` | [MonomialOrder.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/MonomialOrder.lean), [ClosedGraphs.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/ClosedGraphs.lean) | `fij` is the explicit polynomial; `generatorSet` packages the generating family for Gröbner arguments |

## Graph-Theoretic Definitions

| Mathematical object | Lean declaration(s) | File | Notes |
|---|---|---|---|
| Closed graph | `IsClosedGraph` | [Definitions.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/Definitions.lean) | Encodes condition (b) from Theorem 1.1 directly against the fixed linear order on vertices |
| Closure $\bar G$ | `graphClosure`, `graphClosure_isClosedGraph`, `graphClosure_minimal` | [GraphProperties.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GraphProperties.lean) | The unique minimal closed supergraph from Proposition 1.5 |
| Directed shortest-path condition | `IsDirectedWalk` | [GraphProperties.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GraphProperties.lean) | Used in the formalization of Proposition 1.4 |
| Path graphs | `IsPathGraph`, `pathGraph`, `pathGraph_isClosedGraph` | [GraphProperties.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GraphProperties.lean) | Used to package Corollary 1.3 in a paper-faithful connected form |
| Proposition 1.6 hypothesis | `SatisfiesProp1_6Condition` | [Equidim.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/Equidim.lean) | Formalizes the transitivity-style graph condition used in the CM criterion |

## Monomial Order

The paper works with the lexicographic order
$$
x_1 > \cdots > x_n > y_1 > \cdots > y_n.
$$

This is formalized in Lean by first fixing a `LinearOrder` on the variable type
`BinomialEdgeVars V`, and then using Mathlib's lexicographic monomial order.

| Mathematical object | Lean declaration(s) | File | Notes |
|---|---|---|---|
| Monomial order | `binomialEdgeMonomialOrder` | [MonomialOrder.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/MonomialOrder.lean) | Implemented as `MonomialOrder.lex` on `BinomialEdgeVars V →₀ ℕ` |
| Generator leading monomial | `fij_degree` | [MonomialOrder.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/MonomialOrder.lean) | Shows the leading monomial of `fij` is `x_i y_j` for `i < j` |
| Generator leading coefficient | `fij_leadingCoeff`, `fij_leadingCoeff_isUnit` | [MonomialOrder.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/MonomialOrder.lean) | Used to satisfy the Gröbner-basis API hypotheses |

### Lean snippet

```lean
noncomputable def binomialEdgeMonomialOrder : MonomialOrder (BinomialEdgeVars V) :=
  MonomialOrder.lex

noncomputable def fij (i j : V) : MvPolynomial (BinomialEdgeVars V) K :=
  x i * y j - x j * y i

theorem fij_degree (i j : V) (hij : i < j) :
    binomialEdgeMonomialOrder.degree (fij (K := K) i j) =
      Finsupp.single (Sum.inl i) 1 + Finsupp.single (Sum.inr j) 1 := by
```

## Gröbner-Basis Layer

The project uses a small local API over Mathlib's multivariate polynomial
division and `sPolynomial` infrastructure.

| Mathematical object | Lean declaration(s) | File | Notes |
|---|---|---|---|
| Polynomial remainder | `MonomialOrder.IsRemainder` | [GroebnerAPI.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GroebnerAPI.lean) | Encodes remainder computations against a set of generators |
| Gröbner basis predicate | `MonomialOrder.IsGroebnerBasis` | [GroebnerAPI.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GroebnerAPI.lean) | The project-facing predicate used throughout Section 1 and Section 2 |
| Buchberger criterion | `MonomialOrder.isGroebnerBasis_iff_sPolynomial_isRemainder` | [GroebnerAPI.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/GroebnerAPI.lean) | The main bridge used in the formal proof of Theorem 1.1 |
| Closed-graph theorem | `closed_implies_groebner`, `groebner_implies_closed`, `theorem_1_1` | [ClosedGraphs.lean](https://github.com/tom111/BEI-lean/blob/master/BEI/ClosedGraphs.lean) | Main Section 1 theorem in both directions |

## Why This Page Exists

The section pages are theorem maps. This page is different: it is the shared
dictionary underneath those theorem maps.

Use it when you want to know:

- which Lean declaration corresponds to a paper definition;
- where the term order is formalized;
- where the project's Gröbner basis API begins;
- and which files define the common vocabulary reused across Sections 1–4.
